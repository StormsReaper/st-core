(()=>{window.addEventListener('message',e=>{const m=e.data;if(!m||m.action!=='result'||!m.result)return;const r=m.result;if(typeof toast==='function')toast(r.message|| (r.ok?'Vehicle registered successfully.':'Registration failed.'));if(!r.ok)return;const reg=r.data;if(reg&&typeof state==='object'){state.registrations=state.registrations||[];const i=state.registrations.findIndex(x=>String(x.vehicle_identifier)===String(reg.vehicle_identifier));if(i>=0)state.registrations[i]=reg;else state.registrations.unshift(reg);if(typeof render==='function')render();}if(typeof lookupState!=='undefined')lookupState=null;const box=document.getElementById('vehicleLookup');if(box)box.innerHTML='<div class="empty">Vehicle registration completed successfully. Your registration card has been issued.</div>';});})();
